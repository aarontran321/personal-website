// Vercel Web Analytics integration
// This module initializes analytics tracking for the site.
// Import this in each HTML page to enable analytics.

import { inject } from '@vercel/analytics';

// Initialize Vercel Web Analytics
inject();
